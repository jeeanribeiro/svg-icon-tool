# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

Run `pnpm changeset` after a user-facing change to record what kind of
version bump it needs; the release workflow turns accumulated changesets
into a version PR and publishes from it. See RELEASING.md for the full
flow.
